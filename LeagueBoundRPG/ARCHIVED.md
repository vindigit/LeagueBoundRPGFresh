# Archived Folder Notice

`LeagueBoundRPG/` is archived and currently unused.

Do not run Expo from this folder.

The active app is:

`C:\Users\valexander\leaguebound-fresh`

Run the active app from repo root with:

`npx expo start --clear --tunnel`

This folder is retained only as a reversible reference snapshot.
